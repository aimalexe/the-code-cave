apiVersion: v1
kind: Service
metadata:
  labels:
    app: rest-api
  name: rest-api
  namespace: default
spec:
  ports:
  - nodePort: 32667
    port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: rest-api
  sessionAffinity: None
  type: LoadBalancer
