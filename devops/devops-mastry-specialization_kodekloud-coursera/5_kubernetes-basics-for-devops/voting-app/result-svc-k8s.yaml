# Service to expose the Result App (External Access)
apiVersion: v1
kind: Service
metadata:
  name: result-service
  labels:
    app: demo-voting-app
    tier: frontend
  annotations:
    createdBy: "DevOps Team"
    description: "NodePort service exposing the Result App for users."

spec:
  # Selector must match labels (not 'name') from the Result App Pod
  selector:
    app: demo-voting-app
    tier: frontend

  # Defines how the service is exposed
  type: NodePort  # Makes the service accessible outside the cluster

  # Defines port mappings
  ports:
    - port: 80         # Cluster-wide port
      targetPort: 80   # Port on the Result App container
      nodePort: 30005  # External port (Accessible on Node's IP)
